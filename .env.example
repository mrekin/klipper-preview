# Moonraker connection
MOONRAKER_URL=http://192.168.1.XXX:7125

# Database path (inside container)
DATABASE_PATH=/data/tokens.db

# Note: Base path is now configured via Caddy reverse proxy using the X-Base-Path header
# See docs/deployment/reverse-proxy.md for configuration details
