# Moonraker connection
MOONRAKER_URL=http://192.168.1.100:7125

# Database path
DATABASE_PATH=./data/tokens.db

# Note: Base path is configured via Caddy reverse proxy using the X-Base-Path header
# See docs/deployment/reverse-proxy.md for configuration details
# Printers are configured via /admin/settings in the app
